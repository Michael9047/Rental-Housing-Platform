# 2026-08-04 工作总结

## 一、合并同步

### 1.1 Pull & Merge GitHub main
- 拉取 `origin/main`：`78de6ec` → `f2ade53`（140+ commits）
- 合并入 `feat/tenant-detail-and-visit-messages` 分支
- `-X theirs` 策略解决 89 个冲突，完全采用 main 的两层架构（Institute + UnitType）

### 1.2 合并后修复
- `models/poi.py`：删除重复的 `institute_id` 列定义（合并冲突残留）
- `models/unit_type.py`：补 `Float` import + `images` relationship
- `schemas/unit_type.py`：补 `Decimal` import
- `router.py`：补 `me/pois/reviews/images` 等缺失模块导入
- `services/unit_type_service.py`：移除已删除的 `Room` 引用
- Alembic 合并两个分支迁移头
- 数据库补齐缺失列：`unit_types`（property_type/total_count/available_count/has_vacancy）、`institutes`（npc/website_url/building_type/bm_wechat 等 10+ 列）、`bookings`（user_id/bm_id/institute_id 等）、`repair_requests`（user_id/bm_id）、`tenants`（user_id/surname_pinyin/given_name_pinyin/current_unit_type_id 等 30+ 列）

### 1.3 登录修复
- `/api/v1/me` 返回 404：前端 `getMe()` 改为调用正确的 `/auth/me`
- 后端 `me.py` 路由补充 `GET /` 端点
- `App.vue` 全局错误捕获过滤 `ResizeObserver` 良性警告，避免 Vue App Error 覆盖层误弹

---

## 二、左侧菜单重构

### 2.1 菜单项调整
**房东菜单最终6项：**
| 菜单项 | 路由 | 说明 |
|--------|------|------|
| 公寓管理 | `/buildings` | 已有 |
| 户型管理 | `/unit-type/manage` | 已有 |
| 预约管理 | `/bookings/landlord` | 已有 |
| 合约管理 | `/contracts/landlord` | 新增 |
| 租客管理 | `/tenants` | 新增/重写 |
| 维修工单 | `/repairs/manage` | 重写 |

**移除菜单项**（路由保留不删）：
- 运营工作台 `/workspace`（AdminWorkspace 中隐藏 5 个 Tab：房源管理/房源审核/财务中心/消息中心/门店设置）
- 修改记录 `/property/history`
- 消息通知 `/notifications`

### 2.2 涉及文件
- `frontend/src/components/GlobalSidebar.vue`：房东菜单重写
- `frontend/src/router/index.ts`：新增 3 条路由
- `frontend/src/views/admin/AdminWorkspace.vue`：隐藏废弃 Tab

---

## 三、户型管理

### 3.1 库存系统
- 户型卡片新增 **已租/总共** 显示：`(total_count - available_count) / total_count`
- `CreateProperty.vue` 表单新增"总套数"和"可租套数"输入
- 删除了冗余的"可租/已租"状态标签
- 种子数据修复：补充 universities 表数据（新加坡+英国大学），种子脚本 `--clear` 成功生成 32 公寓 111 户型

### 3.2 后端修复
- `_to_read` 补传 `total_count`/`available_count`/`has_vacancy`（之前 Pydantic 用默认值 1 覆盖了真实值）
- `UnitTypeRead` schema 补 `rental_requirements`/`image_urls`/`images`/`room_count` 字段
- 删除重复的 `lease_start_date` 字段
- `UnitTypeUpdate` schema 的 `rental_requirements`/`description` 等字段通过 `getattr` 防御性访问
- `_to_read` 中访问懒加载关系加 `try/except` 防止 `MissingGreenlet`

### 3.3 地图地址回填
- 去掉 `reverseBldGeocode` 中 `!newBuilding.xxx` 旧值保护条件
- 尝试多种响应式方案（watch 桥接、v-if 开关、`:key` 重渲染），均未完美解决 el-dialog 内 Leaflet 回调的响应式问题
- 暂搁置，已加完整报错提示

---

## 四、租客管理

### 4.1 页面功能
- 表格列：**姓名**（surname + given_name 拼接）、电话、学校、租期、所租公寓、所租户型、房间号、状态、操作
- 列宽收紧到 ~960px 总宽，适应 1200px 容器无需横向滚动
- 新增创建/编辑/删除功能，表单支持：
  - 姓（surname_pinyin，必填）+ 名（given_name_pinyin，必填）+ 中文全名（chinese_name，选填）
  - 电话、邮箱、学校
  - **选择公寓** → **分配户型**（两级级联下拉）
  - 房间号、入住/退租日期、居住状态

### 4.2 后端
- `schemas/tenant_order.py`：`TenantCreate`/`TenantUpdate`/`TenantRead` 重写，字段对齐模型
- `routes/tenants.py`：
  - 搜索修 bug：`Tenant.name` 改为 `surname_pinyin`/`given_name_pinyin`
  - **库存联动**：创建租客 → 户型 `available_count - 1`；删除/换户型 → 归还
  - 新增 `DELETE /tenants/{id}` 端点
  - update/create 用 `selectinload` 预加载 `unit_type.institute` 链防止 `MissingGreenlet`
- `models/tenant.py`：新增 `room_number` 列
- 数据库：补齐 tenants 表 30+ 缺失列

### 4.3 前端兼容修复
- `fetchBuildings` 兼容 API 返回数组格式（`Array.isArray(r.data) ? r.data : r.data.items`）

---

## 五、合约管理

### 5.1 房东合约列表
- 新建 `LandlordContracts.vue`：表格显示合同编号/租客姓名/户型/状态/签署日期
- 后端新增 `GET /contracts/landlord`：按房东 `bm_id` 查询 Booking → Contract 链
- 路由顺序修复：`/landlord` 移到 `/{contract_id}` 之前防止被动态路由拦截

### 5.2 合同模板管理系统（新功能）

#### 数据库
- 新增 `contract_templates` 表：id、bm_id、institute_id、name、file_path、field_positions(JSONB)、is_active

#### 后端 API
| 端点 | 功能 |
|------|------|
| `POST /contracts/templates` | BM 上传 PDF 模板（绑定到指定公寓） |
| `GET /contracts/templates` | 模板列表 |
| `GET /contracts/templates/{id}/file` | 下载模板 PDF |
| `PUT /contracts/templates/{id}` | 保存字段坐标 |
| `DELETE /contracts/templates/{id}` | 删除模板 |

#### 前端
- 新建 `ContractTemplateManager.vue`：
  - 左侧：公寓列表 → 选中后右侧显示该公寓的模板
  - 上传 PDF 模板
  - PDF 预览（iframe）+ **拖动框选矩形**标注字段位置
  - 已标字段显示红色边框标记
  - 保存坐标到后端
- `LandlordContracts.vue` 加"📄 模板管理"按钮入口
- 生成 2 个测试 PDF 模板（标准租房合同 + 房间租赁协议）

---

## 六、维修工单

### 6.1 页面整合
- 重写 `MaintenancePlaceholder.vue`（原为空白占位页），改为双 Tab：
  - **📋 工单管理**：状态筛选 + 列表 + 批准/拒绝 + 指派师傅 + 详情
  - **👷 维修师傅**：师傅列表 + 创建 + 状态切换

### 6.2 新增功能
- **批准按钮**：调用 `PATCH /repairs/{id}/status`（PR#31 加的后端端但前端从未调用）
  - `approved` → 映射到 `RepairStatus.assigned`（枚举中无 `approved` 值）
- **拒绝按钮**：`rejected`
- 批准后仍可点"派单"（`pending`/`pending_escalated`/`assigned` 状态均可派单）

### 6.3 测试数据
- 插入 5 条测试工单（水电/家电/门窗/墙面地面/其他，不同状态）
- 修复 `repair_requests` 表指向已删除 `rooms` 表的过期 FK 约束

---

## 七、个人中心

- 新建 `LandlordProfile.vue`：显示/编辑用户名/电话/微信/邮箱/二维码
- 修复二维码上传路径拼接：存完整相对路径，加载时防重复拼接 `/api/v1/uploads/`
- 新增个人中心路由和菜单项

---

## 八、已知遗留问题

1. **地图地址回填**：el-dialog + Leaflet 地图回调中 `reactive()` 赋值无法触发 Vue DOM 更新，尝试 watch 桥接、v-if 开关、:key 重渲染均未解决
2. **合约模板 PDF 填充引擎**：坐标标注已完成，但 PDF 覆盖引擎（PyPDF2 在指定坐标写文本）尚未实现，合同生成时暂未接入模板
3. **维修工单**：批准/拒绝后端已就绪，但需后端进程重启后生效（uvicorn reload 在 Windows 中文路径下不可靠）

---

## 九、文件清单

| 新增文件 | 说明 |
|----------|------|
| `backend/app/models/contract_template.py` | 合同模板模型 |
| `backend/app/schemas/contract_template.py` | 合同模板 Schema |
| `frontend/src/views/LandlordContracts.vue` | 合约列表页 |
| `frontend/src/views/ContractTemplateManager.vue` | 合同模板管理页 |
| `frontend/src/views/LandlordProfile.vue` | 个人中心页 |
| `frontend/src/views/landlord/MaintenancePlaceholder.vue` | 重写：维修工单双Tab |
| `frontend/src/data/currency.ts` | 货币格式化工具 |

| 主要修改文件 | 说明 |
|-------------|------|
| `frontend/src/components/GlobalSidebar.vue` | 菜单重构 |
| `frontend/src/router/index.ts` | 路由调整 |
| `frontend/src/views/TenantManagement.vue` | 租客管理重写 |
| `frontend/src/views/CreateProperty.vue` | 库存字段 |
| `frontend/src/views/ManageProperties.vue` | 库存显示+地图修复 |
| `frontend/src/views/admin/AdminWorkspace.vue` | 隐藏 Tab |
| `backend/app/api/v1/routes/tenants.py` | 租客 CRUD + 库存联动 |
| `backend/app/api/v1/routes/contracts.py` | 房东合约 + 模板 API |
| `backend/app/api/v1/routes/unit_types.py` | _to_read 修复 |
| `backend/app/services/unit_type_service.py` | getattr 防御 |
| `backend/app/schemas/unit_type.py` | Schema 补字段 |
| `backend/app/schemas/tenant_order.py` | Schema 重写 |
| `backend/app/models/tenant.py` | 加 room_number |
