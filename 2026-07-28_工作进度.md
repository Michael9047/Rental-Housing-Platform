# 2026-07-28 工作进度 — 三层ID体系与字段规范化

## 运行环境

| 服务 | 地址 | 状态 |
|------|------|------|
| 前端 Vite | http://localhost:5173 | 🟢 运行中 |
| 后端 FastAPI | http://localhost:8000 | 🟢 运行中 |
| API 文档 | http://localhost:8000/docs | 🟢 |
| PostgreSQL | localhost:5432 | 🟢 pgvector/pg16 |
| Redis | localhost:6379 | 🟢 redis:7 |

启动命令：
```bash
# 终端1: Docker
cd "E:\Project\租房智能体\Rental-Housing-Platform-main"
docker compose up -d

# 终端2: 后端
cd "E:\Project\租房智能体\Rental-Housing-Platform-main\backend"
.venv\Scripts\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 终端3: 前端
cd "E:\Project\租房智能体\Rental-Housing-Platform-main\frontend"
npx vite --host 0.0.0.0 --port 5173
```

---

## 今天完成的改动

### 1. 三层ID体系 (business_id + uuid)

**新增文件**: `backend/app/core/business_id.py`
- 生成规则: `{PREFIX}-{YYYYMMDD}-{4位序号}`
- 前缀: INST(公寓) / UT(户型) / RM(房间)
- 创建时自动查询当日最大序号+1

**模型改动**:
- `institute.py`: business_id String(20)→String(24) + uuid
- `unit_type.py`: 新增 business_id + uuid
- `property.py`: 新增 business_id + uuid

**数据库状态**: 134条记录全部回填完毕
```
institutes: 33 total, 33 has_business_id, 33 has_uuid
unit_types: 24 total, 24 has_business_id, 24 has_uuid
rooms:      77 total, 77 has_business_id, 77 has_uuid
```

**三层ID含义**:
| id | Integer SERIAL | 数据库内部，JOIN/外键 |
| business_id | String(24) UNIQUE | 前端展示，人类可读 |
| uuid | String(36) UNIQUE | 外部对接，永久不变 |

---

### 2. Room 模型清理

**删除的19个废弃列**（已从模型和数据库物理删除）:
`title, address, district, price_monthly, area_sqm, bedrooms, bathrooms, property_type, deposit_amount, deposit_type, service_fee_rate, description, country, latitude, longitude, rent_type, rental_rules, embedding, city`

**删除的冗余列**: `institute_id` — 公寓归属统一走 Room→UnitType→Institute JOIN

**status 改 Enum**: `String(30)` → `Enum(RoomStatus)` — 只能写入 available/pending_review/rented/maintenance/offline

**Room 最终字段 (12个核心列)**:
`id, business_id, uuid, landlord_id, unit_type_id, room_number, building_block, floor, special_discount, available_from, status, min_stay_months`

---

### 3. UnitType 字段规范化

| 变更 | 旧 | 新 |
|------|-----|-----|
| bathrooms 默认值 | 1 | 0 |
| base_rent 类型 | Numeric(12,2) | Float |
| deposit_amount 类型 | Integer | Float |
| area_sqm 类型 | Numeric(8,2) | Float |
| lease_start/end | 仅 String(50) | String(50) + Date 双列 |
| 户型图片 | ARRAY(String) 直接存 | unit_type_images 子表 (与公寓统一) |
| status | 旧枚举名 room_type_status | 新枚举名 unit_type_status |

**新增表**: `unit_type_images` — 与 `building_images` 设计一致 (filename, sort_order, is_primary)

---

### 4. 数据库迁移 (3个)

```
983b708fb08f → 8e815cebb8be  add_business_id_and_remove_room_institute_id
8e815cebb8be → 46533e6f90f2  add_uuid_to_all_three_layers
46533e6f90f2 → 07de8c996fa3  cleanup_room_model_add_unit_type_images (当前)
```

---

### 5. Git

- **分支**: `feat/three-layer-id-system`
- **Commit**: `e0bd9ad` — 23 files, +590/-488
- **Issue**: [#38](https://github.com/Michael9047/Rental-Housing-Platform/issues/38)
- **PR**: [#39](https://github.com/Michael9047/Rental-Housing-Platform/pull/39)
- **状态**: 待人工补充截图后 review

---

## 可视化文件

`E:\Project\租房智能体\公寓模块字段表格.html` — 包含真实数据的可交互三层ID全景图

---

## 明天可以接着做的事

### 如果要继续优化
1. **P0 金额修复**: Booking/Payment 金额 Integer→Numeric/Float（目前只有三层架构的改了）
2. **P0 日期修复**: Booking.scheduled_date, RepairRequest.scheduled_time/completed_at String→Date/DateTime
3. **P1 其他表加ID**: Booking/Order/RepairRequest/Notification 加 business_id + uuid
4. **amenities 枚举**: 定义设施标签标准值，前端多选+校验

### 如果PR合并后有冲突
- 当前分支基于 `feat/structured-address-location`，如果main有新提交需要 rebase
- 注意 `backend/app/api/v1/routes/properties.py` 和 `property_service.py` 改动较大

### 注意事项
- 前端的 `PropertyDetail.vue` 仍会读取 `building.images` — 公寓图片走 `building_images` 表，已正常
- 户型图片从 `ut.image_urls[]` 改成了 `ut.images[]` 子表结构 — 前端如果直接引用 image_urls 需要更新
- Room 的 19 个废弃列如果前端还有引用，需要在 Vue 组件中改为通过 unit_type 链获取

---

## 关键文件改动清单

```
新增:
  backend/app/core/business_id.py
  backend/alembic/versions/8e815cebb8be_add_business_id_and_remove_room_.py
  backend/alembic/versions/46533e6f90f2_add_uuid_to_all_three_layers.py
  backend/alembic/versions/07de8c996fa3_cleanup_room_model_add_unit_type_images.py

重写:
  backend/app/models/property.py      (Room模型：28列→12列)
  backend/app/models/unit_type.py     (UnitType重构 + UnitTypeImage子表)
  backend/app/schemas/room.py
  backend/app/schemas/unit_type.py
  backend/app/services/room_service.py
  backend/app/services/unit_type_service.py
  backend/app/api/v1/routes/unit_types.py

修改:
  backend/app/models/institute.py     (uuid + business_id类型)
  backend/app/schemas/property.py
  backend/app/services/property_service.py
  backend/app/services/comparison_data.py
  backend/app/services/review_service.py
  backend/app/services/import_service.py
  backend/app/services/agentic/agents/compare_agent.py
  backend/app/api/v1/routes/buildings.py
  backend/app/api/v1/routes/properties.py
  backend/app/api/v1/routes/rooms.py
  backend/app/api/v1/routes/search_suggestions.py
  backend/app/api/v1/router.py
```
